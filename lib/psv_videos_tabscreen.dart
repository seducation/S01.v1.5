import 'package:flutter/material.dart';

class PsvVideosTabscreen extends StatelessWidget {
  const PsvVideosTabscreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Videos Content"),
    );
  }
}
