import 'package:flutter/material.dart';

class HMVFollowingTabscreen extends StatelessWidget {
  const HMVFollowingTabscreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Following'),
    );
  }
}
