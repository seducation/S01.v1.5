import 'package:flutter/material.dart';

class PsvLiveTabscreen extends StatelessWidget {
  const PsvLiveTabscreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Live Content"),
    );
  }
}
