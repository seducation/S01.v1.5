import 'package:flutter/material.dart';

class HMVVideosTabscreen extends StatelessWidget {
  const HMVVideosTabscreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Videos'),
    );
  }
}
