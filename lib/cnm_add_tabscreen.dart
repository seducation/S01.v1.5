import 'package:flutter/material.dart';

class CNMAddTabscreen extends StatelessWidget {
  const CNMAddTabscreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Add Friends'),
      ),
      body: const Center(
        child: Text('Add Friends Screen'),
      ),
    );
  }
}
