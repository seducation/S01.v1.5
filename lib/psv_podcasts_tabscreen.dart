import 'package:flutter/material.dart';

class PsvPodcastsTabscreen extends StatelessWidget {
  const PsvPodcastsTabscreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Podcasts Content"),
    );
  }
}
