import 'package:flutter/material.dart';

class PsvAboutTabscreen extends StatelessWidget {
  const PsvAboutTabscreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("About Content"),
    );
  }
}
