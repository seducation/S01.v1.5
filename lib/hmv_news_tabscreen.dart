import 'package:flutter/material.dart';

class HMVNewsTabscreen extends StatelessWidget {
  const HMVNewsTabscreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('News'),
    );
  }
}
